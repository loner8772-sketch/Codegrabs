<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Code Grabber"
        android:theme="@style/Theme.CodeGrabber">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RedirectCountdownActivity"
            android:exported="false"
            android:theme="@style/Theme.CodeGrabber"
            android:showOnLockScreen="true"
            android:launchMode="singleTop" />

        <activity
            android:name=".RedeemWebViewActivity"
            android:exported="false"
            android:theme="@style/Theme.CodeGrabber" />

        <service
            android:name=".CodeListenerService"
            android:label="Code Grabber Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
